# Claude Repository Instructions

Before editing, read `AGENTS.md`, the nearest scoped instruction file, and relevant documents under `/instructions`.

Do not introduce CQRS, MediatR, an event broker, authentication, containers, or new infrastructure unless the current task explicitly requests it. Preserve Clean Architecture dependency direction. Run build and tests and report evidence.
