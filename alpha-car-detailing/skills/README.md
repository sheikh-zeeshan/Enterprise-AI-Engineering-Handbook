# Skill Library

Skills are reusable engineering procedures. They define how recurring work is performed while persistent standards remain under `/instructions`.

The current library deliberately uses `add-application-service` instead of a CQRS command/query Skill. CQRS will be added after the working baseline is stable.
